import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "User profile",
};

function Profile() {
  return (
    <div>
      <p>User Profile</p>
    </div>
  );
}

export default Profile;
